DATABASE_URL=postgresql+psycopg2://ascent:ascent@localhost:5432/ascent
JWT_SECRET=change-me-in-production

# AI coach (phase 3). Without this the /coach endpoints return 503; everything
# else keeps working. https://platform.claude.com/settings/keys
ANTHROPIC_API_KEY=
